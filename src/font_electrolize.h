#pragma once
// Placeholder — overwritten by CI at build time with the real TTF bytes.
// DO NOT edit. The CI step writes the real data before compilation.
static const unsigned char electrolize_regular_ttf[]   = { 0 };
static const unsigned int  electrolize_regular_ttf_len = 0;
